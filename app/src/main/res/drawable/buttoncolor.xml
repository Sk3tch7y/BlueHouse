<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    Remember to have the state_pressed at the top of the list, else the app will crash immediately upon starting up.
    -->

    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/blue"/>
            <corners android:radius="5dp"/>
        </shape>
    </item>
    <item android:state_enabled="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/white"/>
            <corners android:radius="5dp"/>
        </shape>
    </item>
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/black"/>
            <corners android:radius="5dp"/>
        </shape>
    </item>


<!--
This is the XML file to change the look of the button when it has been clicked or not!


to enable this as the background, you have to go to -> VALUES -> THEMES -> and change to "Theme.AppCompat.DayNight.NoActionBar"
else whatever changes you put into this XML file, will not be enabled once you set this file as the Button background

-->


</selector>